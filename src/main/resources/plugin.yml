name: Prevent
version: '${project.version}'
main: net.achymake.prevent.Prevent
api-version: 1.15
prefix: '${project.name}'
load: POSTWORLD
author: AchyMake
description: '${project.description}'
commands:
  prevent:
    description: message
    permission: prevent.command
    usage: /prevent
    permission-message: You do not have the permission to execute the command
permissions:
  prevent.command:
    description: reload config
    default: op
