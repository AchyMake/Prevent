name: Prevent
version: '1.19'
main: net.achymake.prevent.Prevent
api-version: 1.19
prefix: 'Prevent'
load: POSTWORLD
author: AchyMake
description: 'Message'
commands:
  prevent:
    description: message
    permission: prevent.command.reload
    usage: /prevent reload
    permission-message: You do not have the permission to execute the command
permissions:
  prevent.command.reload:
    description: reload config
    default: op
